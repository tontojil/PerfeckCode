# Prompt Keywords (30)

1. `sticky navigation bar` — ej: "sticky navigation bar with centered logo"
2. `hero section` — ej: "hero section with headline, subtext, primary CTA"
3. `card grid` — ej: "card grid, 3 columns, equal height"
4. `glassmorphism` — ej: "glassmorphism header, blur background"
5. `pill button` — ej: "pill button labeled 'Get started'"
6. `ghost button` — ej: "ghost button next to primary CTA"
7. `sidebar layout` — ej: "sidebar layout with collapsible nav"
8. `data table` — ej: "data table with sortable columns"
9. `stat card` — ej: "stat card with big number, delta chip"
10. `accordion FAQ` — ej: "accordion FAQ, expand on click"
11. `testimonial carousel` — ej: "testimonial carousel with avatars"
12. `pricing tiers` — ej: "pricing tiers, highlight popular plan"
13. `masonry grid` — ej: "masonry image grid with captions"
14. `drawer navigation` — ej: "drawer navigation for mobile"
15. `footer columns` — ej: "footer with 4 link columns"
16. `auth card` — ej: "centered auth card, email + password"
17. `alert banner` — ej: "dismissible alert banner, info role"
18. `hover shadow` — ej: "card with soft hover shadow"
19. `hairline border` — ej: "card with hairline border, 16px radius"
20. `uppercase nav` — ej: "uppercase nav links, tracking wide"
21. `active underline` — ej: "active link with underline indicator"
22. `stacked form` — ej: "stacked form with labeled inputs"
23. `filter bar` — ej: "filter bar above product grid"
24. `cart drawer` — ej: "cart drawer sliding from right"
25. `featured post` — ej: "featured post hero, 2-col layout"
26. `avatar stack` — ej: "avatar stack with +12 overflow chip"
27. `empty state` — ej: "empty state with icon, CTA"
28. `skeleton loader` — ej: "skeleton loader for card grid"
29. `toast` — ej: "success toast bottom-right, auto-dismiss"
30. `tabs` — ej: "tabs switching pricing monthly/yearly"
